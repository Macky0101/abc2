export interface DomainCard {
  icon: string;
  title: string;
  subtitle?: string;
  description: string;
  iconColor: string;
}

export interface SectorCard {
  image: string;
  title: string;
  description: string;
}

export interface NavItem {
  label: string;
  href: string;
}
